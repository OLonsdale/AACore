mogrify -format webp *.png

firebase deploy

for (const board in boards) {
  if (Object.hasOwnProperty.call(boards, board)) {
    const element = boards[board];

    element.tiles.forEach(tile => {
      if(tile.type === "blank") return
      library[tile.internalName] = tile
    })
    
  }
}


ctrl + alt + s -> w
Update service worker before deploying
